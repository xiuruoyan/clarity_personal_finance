# The current demo runs without environment variables.
# Add these only when implementing the server-side Plaid integration.

PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox
PLAID_REDIRECT_URI=http://localhost:3000/api/plaid/oauth-return
PLAID_WEBHOOK_URL=
